# Автоматическая синхронизация статусов из Мастер-карты

## 📋 Описание функциональности

Реализована автоматическая подтяжка статусов строительных объектов из Мастер-карты стратегического развития во все графики отделов.

## ✨ Что было сделано

### Backend (API)

1. **Обновлен эндпоинт `/api/cities`:**
   - При загрузке списка объектов автоматически подтягивается `current_status` из таблицы `strategic_map_projects`
   - Статус определяется по полю `current_status` проекта, привязанного к объекту через `city_id`

2. **Обновлена схема City:**
   - Добавлено поле `current_status: Optional[str]` для хранения текущего статуса объекта

### Frontend

#### 1. Компонент StatusBadge (`frontend/src/components/StatusBadge.js`)

Создан универсальный компонент для отображения статусов с цветовой индикацией:

**Поддерживаемые статусы и цвета:**
- 🟢 **Завершен** - зеленый (#22c55e)
- 🟠 **Строительство** - оранжевый (#f59e0b)
- 🔵 **Проектирование** - синий (#3b82f6)
- 🟣 **Согласование** - фиолетовый (#8b5cf6)
- 🔷 **Подготовка** - голубой (#06b6d4)
- 🟦 **Поиск участка** - индиго (#6366f1)
- 🟪 **Приобретение прав на участок** - пурпурный (#a855f7)
- 🎀 **Есть право на зу, ждет реализации** - розовый (#ec4899)
- 🧪 **Проектирование, экспертиза, получение РНС** - бирюзовый (#14b8a6)

#### 2. Страница "Управление объектами строительства" (`Cities.js`)

**Изменения:**
- ✅ Добавлен столбец "Статус" в таблице объектов
- ✅ Статус отображается в виде цветного бейджа
- ✅ Если статус не установлен, отображается "—"

**Пример отображения:**
```
ID | Название      | Описание | Статус           | Видимость | Дата      | Действия
1  | Павловск 3    | ...      | [Строительство]  | ✓         | 16.01.26  | [✏️] [🗑️]
2  | Богучар №2    | ...      | [Завершен]       | ✓         | 20.01.26  | [✏️] [🗑️]
```

#### 3. Графики отделов

Обновлены все графики отделов для отображения статуса объекта:

**Обновленные страницы:**
- ✅ HR-график (`HRSchedule.js`)
- ✅ График выдачи документации (`DocumentSchedule.js`)
- ✅ График закупок (`ProcurementSchedule.js`)
- ✅ График строительства (`ConstructionSchedule.js`)
- ✅ График маркетинга (`MarketingSchedule.js`)
- ✅ Директивный график (`DirectiveSchedule.js`)

**Как отображается:**

Статус показывается как маленький бейдж рядом с названием объекта в кнопках выбора:

```
┌─────────────────────────────────┐
│ Павловск 3 [Строительство]      │  ← Активный объект (синий фон)
└─────────────────────────────────┘
┌─────────────────────────────────┐
│ Богучар №2 [Завершен]           │  ← Неактивный (серый фон)
└─────────────────────────────────┘
```

**Характеристики бейджа:**
- Компактный размер (10px шрифт, минимальный padding)
- Цветовое кодирование по типу статуса
- Всплывающая подсказка с полным названием статуса при наведении
- Ненавязчивое расположение справа от названия объекта

## 🔄 Принцип работы

### Поток данных:

```
Мастер-карта (current_status в strategic_map_projects)
           ↓
    Backend API (/cities)
           ↓
  Frontend (cities state)
           ↓
    ┌──────────────┬──────────────┐
    ↓              ↓              ↓
Cities.js    HR-график    Закупки    ... и др.
```

### Автоматическое обновление:

1. **При изменении статуса в Мастер-карте:**
   - Статус сохраняется в поле `current_status` проекта
   - При следующей загрузке списка объектов статус автоматически подтягивается

2. **Отображение в интерфейсе:**
   - Во вкладке "Управление объектами" - в отдельном столбце
   - В графиках отделов - как бейдж рядом с названием объекта

## 🎨 UX решения

### Почему именно так:

1. **Ненавязчивость:**
   - Статус не занимает много места
   - Не перегружает интерфейс
   - Виден только при необходимости

2. **Цветовая индикация:**
   - Быстрое визуальное определение статуса
   - Единая цветовая схема во всех частях системы

3. **Консистентность:**
   - Одинаковое отображение везде
   - Общий компонент `StatusBadge`

## 📝 Примеры использования

### Установка статуса в Мастер-карте:

1. Откройте "Мастер-карта стратегического развития"
2. Выберите объект
3. Установите "Текущий статус" из выпадающего списка
4. Статус автоматически появится во всех графиках

### Просмотр статуса:

**В управлении объектами:**
- Столбец "Статус" показывает текущее состояние каждого объекта

**В графиках отделов:**
- Бейдж рядом с названием объекта
- Наведите курсор для полного названия статуса

## 🔧 Техническая информация

### Файлы изменений:

**Backend:**
- `backend/app/routers/cities.py` - логика подтяжки статуса
- `backend/app/schemas.py` - добавлено поле `current_status`

**Frontend:**
- `frontend/src/components/StatusBadge.js` - новый компонент
- `frontend/src/pages/Cities.js` - столбец статуса
- `frontend/src/pages/HRSchedule.js` - бейдж в кнопках
- `frontend/src/pages/DocumentSchedule.js` - бейдж в кнопках
- `frontend/src/pages/ProcurementSchedule.js` - бейдж в кнопках
- `frontend/src/pages/ConstructionSchedule.js` - бейдж в кнопках
- `frontend/src/pages/MarketingSchedule.js` - бейдж в кнопках
- `frontend/src/pages/DirectiveSchedule.js` - импорт компонента

### Коммиты:

```bash
9352032 Add automatic status sync from Strategic Map to Cities and Schedules
```

## 🚀 Дальнейшие улучшения

Возможные направления развития:

1. **Фильтрация по статусу:**
   - Добавить возможность фильтровать объекты по статусу в графиках

2. **История изменений:**
   - Отслеживание изменений статуса объекта

3. **Уведомления:**
   - Telegram-уведомления при смене статуса объекта

4. **Дашборд по статусам:**
   - Сводная статистика по статусам всех объектов
