# Фильтр по годам в диаграмме Ганта

## 📅 Обзор

Добавлена функциональность фильтрации по годам в диаграммы Ганта, аналогично фильтру в Мастер-карте стратегического развития.

## ✨ Возможности

### Фильтр по году
- **Выпадающий список** с годами для быстрой фильтрации
- **Опция "Все годы"** для отображения всех задач
- **Автоматическое определение** доступных годов из данных задач
- **Умная фильтрация**: показывает задачи, которые:
  - Начинаются в выбранном году
  - Заканчиваются в выбранном году
  - Выполняются в выбранном году (даже если начались раньше/позже)

### Адаптация временной шкалы
- При выборе года **временная шкала автоматически ограничивается** выбранным годом
- Диапазон даты начинается за месяц до и заканчивается через 2 месяца после
- Оптимальное отображение без лишних пустых месяцев

## 🎯 Где работает

Фильтр по годам работает **во всех** диаграммах Ганта:
- 🏠 **Главная страница** (Dashboard) - общая диаграмма всех отделов
- 🏗️ **График строительства** (Construction Schedule)
- 📄 **График технического заказчика** (Document Schedule)
- 👥 **График HR** (HR Schedule)
- 🛒 **График закупок** (Procurement Schedule)
- 📢 **График маркетинга** (Marketing Schedule)
- 📋 **Директивный график** (Directive Schedule)

## 🔧 Реализация

### Обновлённые файлы

**`frontend/src/components/Dashboard/ModernGanttChart.js`**

#### 1. Добавлен state для фильтра
```javascript
const [yearFilter, setYearFilter] = useState('all'); // Фильтр по году
```

#### 2. Вычисление доступных годов
```javascript
const availableYears = useMemo(() => {
  if (tasks.length === 0) return [];
  const years = new Set();
  tasks.forEach(task => {
    if (task.plannedStart) years.add(task.plannedStart.getFullYear());
    if (task.plannedEnd) years.add(task.plannedEnd.getFullYear());
    if (task.actualStart) years.add(task.actualStart.getFullYear());
    if (task.actualEnd) years.add(task.actualEnd.getFullYear());
  });
  return Array.from(years).sort((a, b) => a - b);
}, [tasks]);
```

#### 3. Фильтрация задач по году
```javascript
const filteredTasks = useMemo(() => {
  if (yearFilter === 'all') return tasks;
  const year = parseInt(yearFilter);
  return tasks.filter(task => {
    const startYear = task.plannedStart?.getFullYear();
    const endYear = task.plannedEnd?.getFullYear();
    const actualStartYear = task.actualStart?.getFullYear();
    const actualEndYear = task.actualEnd?.getFullYear();
    
    return startYear === year || endYear === year || 
           actualStartYear === year || actualEndYear === year ||
           (startYear && endYear && startYear <= year && endYear >= year);
  });
}, [tasks, yearFilter]);
```

#### 4. Адаптация временной шкалы
```javascript
const timeRange = useMemo(() => {
  if (filteredTasks.length === 0) return { start: new Date(), end: new Date(), days: 0 };
  
  let minDate = new Date(Math.min(...filteredTasks.map(t => t.plannedStart.getTime())));
  let maxDate = new Date(Math.max(...filteredTasks.map(t => t.plannedEnd.getTime())));
  
  // Если фильтр по году активен, ограничиваем диапазон этим годом
  if (yearFilter !== 'all') {
    const year = parseInt(yearFilter);
    const yearStart = new Date(year, 0, 1);
    const yearEnd = new Date(year, 11, 31);
    minDate = minDate < yearStart ? yearStart : minDate;
    maxDate = maxDate > yearEnd ? yearEnd : maxDate;
  }
  
  minDate = new Date(minDate.getFullYear(), minDate.getMonth() - 1, 1);
  maxDate = new Date(maxDate.getFullYear(), maxDate.getMonth() + 2, 0);
  
  const days = Math.ceil((maxDate - minDate) / (1000 * 60 * 60 * 24));
  return { start: minDate, end: maxDate, days };
}, [filteredTasks, yearFilter]);
```

#### 5. UI компонент выбора года
```javascript
{/* Фильтр по году */}
<div style={{ display: 'flex', alignItems: 'center', gap: '8px' }}>
  <span style={{ fontSize: '12px', color: '#64748b' }}>Год:</span>
  <select
    value={yearFilter}
    onChange={(e) => setYearFilter(e.target.value)}
    style={{
      padding: '6px 12px',
      borderRadius: '6px',
      border: '1px solid #e2e8f0',
      background: '#fff',
      fontSize: '13px',
      color: '#1e293b',
      cursor: 'pointer',
      minWidth: '120px'
    }}
  >
    <option value="all">📅 Все годы</option>
    {availableYears.map(year => (
      <option key={year} value={year}>{year}</option>
    ))}
  </select>
</div>
```

## 🎨 UI/UX

### Расположение
Фильтр по году размещён на панели управления диаграммой Ганта:
- Справа от **сортировки**
- Слева от кнопки **"Критический путь"**

### Стиль
- Единообразный дизайн с остальными элементами управления
- Минимальная ширина 120px для удобного чтения
- Иконка 📅 для визуальной идентификации

## 📊 Примеры использования

### Пример 1: Просмотр задач 2025 года
1. Откройте диаграмму Ганта (любой отдел)
2. В выпадающем списке "Год" выберите **2025**
3. Результат: отображаются только задачи 2025 года
4. Временная шкала показывает январь - декабрь 2025

### Пример 2: Просмотр всех задач
1. В выпадающем списке "Год" выберите **📅 Все годы**
2. Результат: отображаются все задачи всех годов
3. Временная шкала охватывает весь диапазон дат

### Пример 3: Сравнение разных годов
1. Выберите **2024** → анализируйте задачи
2. Выберите **2025** → сравните с планами на следующий год
3. Выберите **2026** → просмотрите долгосрочные планы

## 🔄 Взаимодействие с другими функциями

### Совместимость
Фильтр по году работает **совместно** со всеми остальными функциями:
- ✅ **Сортировка** (по дате, названию, этапу и т.д.)
- ✅ **Масштаб** (день, неделя, месяц)
- ✅ **Критический путь** (фильтруется вместе с задачами)
- ✅ **Перетаскивание блоков** (работает в отфильтрованном виде)
- ✅ **Изменение размера блоков** (работает корректно)

### Счётчики
Все счётчики автоматически обновляются:
- **"Задачи (N)"** в заголовке - показывает количество отфильтрованных задач
- **Критических задач** - показывает количество в выбранном году

## ⚡ Производительность

- **Мемоизация**: используется `useMemo` для оптимизации
- **Умная фильтрация**: пересчёт только при изменении задач или фильтра
- **Минимальные перерисовки**: React оптимизирует обновления

## 🧪 Тестирование

### Тест 1: Проверка доступных годов
1. Создайте задачи с датами в разных годах (2024, 2025, 2026)
2. Откройте диаграмму Ганта
3. ✅ В списке должны появиться все годы с задачами

### Тест 2: Фильтрация по году
1. Выберите конкретный год
2. ✅ Отображаются только задачи этого года
3. ✅ Счётчик "Задачи (N)" обновился
4. ✅ Временная шкала ограничена выбранным годом

### Тест 3: Переключение фильтра
1. Выберите 2025
2. Выберите "Все годы"
3. Снова выберите 2025
4. ✅ Фильтр работает корректно при переключении

### Тест 4: Перетаскивание с фильтром
1. Выберите конкретный год
2. Перетащите блок задачи на новую дату
3. ✅ Изменения сохраняются
4. ✅ Блок остаётся на новом месте

## 📝 Примечания

- Фильтр **не сохраняется** между сессиями (по умолчанию "Все годы")
- Если год не имеет задач, он **не отображается** в списке
- Годы в списке отсортированы **по возрастанию**
- Логика фильтрации **идентична** Мастер-карте для единообразия

## 🚀 Будущие улучшения

Возможные дополнения (опционально):
- Сохранение выбранного года в localStorage
- Мультивыбор годов (например, 2024-2025)
- Ползунок для выбора диапазона лет (как в Мастер-карте)
- Быстрые пресеты: "Текущий год", "Следующий год", "Прошлый год"

---

**Статус**: ✅ Реализовано и работает
**Дата**: 27.01.2026
