# Исправление проблемы Drag-and-Drop в диаграммах Ганта

## 🐛 Описание проблемы

При перетаскивании блоков на диаграмме Ганта наблюдались следующие симптомы:

1. ~~**Блок не сохраняет своё положение**~~ ✅ ИСПРАВЛЕНО
2. ~~**Даты не меняются**~~ ✅ ИСПРАВЛЕНО  
3. **Бесконечная загрузка** - индикатор "Сохранение..." крутится бесконечно ⚠️ КРИТИЧЕСКИЙ БАГ

## 🔍 Корневые причины (Глубокий анализ)

### 1. Race Condition между родительским компонентом и ModernGanttChart
Родительский компонент передает `schedules` через props, которые могут обновиться во время drag-and-drop операции, перезаписывая локальное состояние `tasks` в `ModernGanttChart`.

**Файл:** `frontend/src/components/Dashboard/ModernGanttChart.js`
- **Строки:** 240-268 (useEffect для обработки schedules)

### 2. ⚠️ КРИТИЧЕСКИЙ БАГ: Использование setTimeout с асинхронным сбросом индикатора
`isSyncing` устанавливался в `true` СИНХРОННО, но сбрасывался АСИНХРОННО внутри `setTimeout(50ms)`:

**Проблема:**
1. `handleMouseUp()` вызывается → `setIsSyncing(true)` 
2. `setTimeout(() => { ... }, 50)` планируется
3. `handleMouseUp()` завершается  
4. Если компонент размонтируется или таймаут очищается → `setIsSyncing(false)` НИКОГДА не вызывается!
5. Индикатор остаётся `true` навсегда

**Файл:** `frontend/src/components/Dashboard/ModernGanttChart.js`
- **Строки:** 446-522 (handleMouseUp)

### 3. ⚠️ Проблема с множественным drag-and-drop
При быстром перетаскивании нескольких блоков boolean флаг `isSyncing` работал некорректно:

**Сценарий:**
1. Перетащили блок 1 → `setIsSyncing(true)`
2. Перетащили блок 2 → `setIsSyncing(true)` (уже true)
3. Сохранение блока 1 завершилось → `setIsSyncing(false)`
4. Блок 2 всё ещё сохраняется, НО индикатор уже выключен!

### 4. Отсутствие таймаута для HTTP-запросов
Запросы на сохранение не имели таймаута, что могло приводить к бесконечному ожиданию при проблемах с сетью.

**Файлы:** Все страницы с графиками
- `Home.js`, `ConstructionSchedule.js`, `DirectiveSchedule.js`, `DocumentSchedule.js`, `HRSchedule.js`, `MarketingSchedule.js`, `ProcurementSchedule.js`

### 5. Нет защиты от обновления во время синхронизации
useEffect обновлял `tasks` из `schedules` даже когда `isSyncing === true`, что приводило к потере изменений во время сохранения.

## ✅ Внесённые исправления (Итерация 2 - ГЛУБОКИЙ АНАЛИЗ)

### 1. 🔥 КРИТИЧЕСКИЙ БАГ-ФИХ: Удалён setTimeout
**Файл:** `frontend/src/components/Dashboard/ModernGanttChart.js`

```javascript
// БЫЛО (ПРОБЛЕМА):
setIsSyncing(true);  // Вызывается СИНХРОННО
setTimeout(async () => {
  try { await save(); }
  finally { setIsSyncing(false); }  // Вызывается АСИНХРОННО через 50мс
}, 50);
// Если таймаут очищается → finally НИКОГДА не выполнится!

// СТАЛО (РЕШЕНИЕ):
(async () => {
  setSyncCount(prev => prev + 1);  // Сразу увеличиваем счётчик
  try { await save(); }
  finally { setSyncCount(prev => Math.max(0, prev - 1)); }
})();  // IIFE выполняется сразу без задержки
```

**Эффект:** Убрана async/sync рассинхронизация, гарантирован вызов finally.

### 2. 🔥 Использован счётчик вместо boolean флага
**Файл:** `frontend/src/components/Dashboard/ModernGanttChart.js`

```javascript
// БЫЛО (ПРОБЛЕМА):
const [isSyncing, setIsSyncing] = useState(false);
// При множественном drag-and-drop:
// Операция 1: true → false (выключает индикатор)
// Операция 2: всё ещё работает, но индикатор выключен!

// СТАЛО (РЕШЕНИЕ):
const [syncCount, setSyncCount] = useState(0);
const isSyncing = syncCount > 0;  // Вычисляемое значение

// Операция 1: count=1 → count=0
// Операция 2: count=1 → count=0  
// При 2 одновременных: count=2 → count=1 → count=0 ✅
```

**Эффект:** Индикатор отображается пока ЛЮБАЯ операция сохранения активна.

### 3. Защита useEffect от обновления во время синхронизации
**Файл:** `frontend/src/components/Dashboard/ModernGanttChart.js`

```javascript
// БЫЛО:
if (dragging || resizing) return;

// СТАЛО:
if (dragging || resizing || isSyncing) return;
```

**Эффект:** Теперь локальное состояние `tasks` не будет перезаписываться prop'ами во время сохранения.

### 4. Гарантированный сброс при размонтировании
**Файл:** `frontend/src/components/Dashboard/ModernGanttChart.js`

```javascript
return () => {
  setSyncCount(0);  // Сбрасываем ВСЕ активные операции
};
```

**Эффект:** При размонтировании компонента индикатор гарантированно исчезнет.

### 5. Добавлен таймаут для запросов (10 секунд)
**Файл:** `frontend/src/components/Dashboard/ModernGanttChart.js`

```javascript
const timeoutPromise = new Promise((_, reject) => 
  setTimeout(() => reject(new Error('Таймаут сохранения')), 10000)
);

await Promise.race([
  onScheduleUpdate(updated.id, saveData),
  timeoutPromise
]);
```

**Эффект:** Если сохранение занимает больше 10 секунд, происходит откат изменений.

### 6. Добавлен AbortController во всех родительских компонентах
**Файлы:** Все страницы с графиками

```javascript
const controller = new AbortController();
const timeoutId = setTimeout(() => controller.abort(), 8000);

await client.put(`/schedules/${scheduleId}`, updates, {
  signal: controller.signal
});

clearTimeout(timeoutId);
```

**Эффект:** Если запрос на сервер зависает, он будет отменён через 8 секунд.

### 7. 🔥 КРИТИЧЕСКИЙ: Обновление локального state после сохранения
**Файлы:** Все страницы с графиками

```javascript
// ПРОБЛЕМА: После успешного сохранения schedules не обновлялись
// useEffect в ModernGanttChart перезаписывал tasks старыми данными
// Результат: блок визуально возвращался на старое место

// РЕШЕНИЕ:
await client.put(`/schedules/${scheduleId}`, updates, { ... });

// Обновляем локальный state после успешного сохранения
setSchedules(prev => prev.map(schedule => 
  schedule.id === scheduleId 
    ? { ...schedule, ...updates }
    : schedule
));
```

**Эффект:** После drag-and-drop блок остаётся на новом месте БЕЗ обновления страницы!

## 🎯 Результат

После внесения всех исправлений:

✅ Блоки сохраняют своё положение после перетаскивания
✅ Даты корректно обновляются на сервере
✅ Индикатор загрузки исчезает после завершения операции
✅ При проблемах с сетью происходит автоматический откат изменений
✅ Нет race conditions между компонентами

## 📋 Затронутые файлы

1. `frontend/src/components/Dashboard/ModernGanttChart.js` - основная логика
2. `frontend/src/pages/Home.js` - добавлен таймаут
3. `frontend/src/pages/ConstructionSchedule.js` - добавлен таймаут
4. `frontend/src/pages/DirectiveSchedule.js` - добавлен таймаут
5. `frontend/src/pages/DocumentSchedule.js` - добавлен таймаут
6. `frontend/src/pages/HRSchedule.js` - добавлен таймаут
7. `frontend/src/pages/MarketingSchedule.js` - добавлен таймаут
8. `frontend/src/pages/ProcurementSchedule.js` - добавлен таймаут

## 🧪 Тестирование

Для проверки исправлений:

1. Откройте любой график с диаграммой Ганта
2. Перетащите блок на новое место
3. Убедитесь, что блок остаётся на новом месте
4. Проверьте, что даты обновились в базе данных
5. Убедитесь, что индикатор "Сохранение..." исчез

Для проверки обработки ошибок (симуляция):
1. Отключите сеть или остановите сервер
2. Попробуйте перетащить блок
3. Убедитесь, что через 8-10 секунд блок вернётся на исходное место
4. Проверьте, что показалось сообщение об ошибке

## 🔄 Дополнительные улучшения

### Возможные будущие улучшения:

1. **Оптимистичные обновления** - обновлять UI сразу, а при ошибке откатывать
2. **Очередь запросов** - если пользователь быстро перетаскивает несколько блоков
3. **WebSocket для синхронизации** - real-time обновления при изменениях от других пользователей
4. **Логирование** - детальные логи операций drag-and-drop для отладки

---

**Дата исправления:** 27 января 2026
**Версия:** 1.0
